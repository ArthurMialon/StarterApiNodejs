/*---------------------------------------------/
| Export all options
| Check your global configuration to change the
| sort of your options and middleware
|----------------------------------------------*/

export default {
  "need": require('./need'),
  "auth": require('./auth')
};
